var minhaVar = 'minha Variavel';

let numero = 2;
const pi = 3.14;

var numeris = [1,2,3];
numeris.map(function(v){
    return v * 2;
})

numeris.map(valor => valor *2);

class Matematica{
    soma(x,y){
        return x+y;
    }
}

var n1: string = "ssss";

const mul = (x, y) =>  x + y;

console.log(mul(4,5));